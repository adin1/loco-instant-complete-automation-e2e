# Environment
NODE_ENV=production

# Connection to the same database
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=loco_instant

# Optional queue / messaging (dacă ai unul definit)
REDIS_HOST=redis
REDIS_PORT=6379

# Example service link to backend
API_URL=http://backend:3000
