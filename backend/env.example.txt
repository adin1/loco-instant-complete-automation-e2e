# ==============================================
# LOCO INSTANT - VARIABILE DE MEDIU (EXEMPLU)
# ==============================================
# Copiază acest fișier ca .env și completează valorile
# ==============================================

# Baza de date PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/loco?schema=public

# Redis
REDIS_URL=redis://localhost:6379

# JWT Secret (minim 32 caractere)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-chars

# Mod aplicație
NODE_ENV=development

# CORS (origini permise, separate prin virgulă)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Rate Limiting
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000

# Port server
PORT=3000

